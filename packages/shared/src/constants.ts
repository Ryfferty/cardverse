/** Default max effect execution steps before forced termination */
export const DEFAULT_MAX_EFFECT_STEPS = 1000;

/** Default response timeout in seconds */
export const DEFAULT_RESPONSE_TIMEOUT = 30;

/** Default reconnect timeout in seconds (1-2 minutes) */
export const DEFAULT_RECONNECT_TIMEOUT = 90;

/** Framework version */
export const FRAMEWORK_VERSION = "0.1.0";

/** Supported deck manifest version */
export const DECK_MANIFEST_VERSION = "1.0.0";
